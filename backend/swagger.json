{
    "swagger": "2.0",
    "info": {
      "version": "1.0.0",
      "title": "My MERN API",
      "description": "Documentation for my MERN API"
    },
    "basePath": "/api",
    "schemes": [
      "http",
      "https"
    ],
    "consumes": [
      "application/json"
    ],
    "produces": [
      "application/json"
    ],
    "paths": {
      "/AddUser": {
        "post": {
          "tags": [
            "users"
          ],
          "summary": "Adds a new user",
          "parameters": [
            {
              "name": "body",
              "in": "body",
              "description": "User object that needs to be added to the database",
              "required": true,
              "schema": {
                "$ref": "#/definitions/User"
              }
            }
          ],
          "responses": {
            "200": {
              "description": "OK"
            },
            "400": {
              "description": "Invalid input"
            }
          }
        }
      }
    },
    "definitions": {
      "User": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "email": {
            "type": "string"
          },
          "firebaseUID": {
            "type": "string"
          }
        },
        "required": [
          "name",
          "email",
          "firebaseUID"
        ]
      }
    }
  }
  